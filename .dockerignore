# ============================================
# Docker Ignore File
# ============================================
# Files and directories that should NOT be copied to the Docker image.
# This speeds up builds and reduces image size.

# Dependencies - installed fresh inside container
node_modules

# Next.js build output - rebuilt inside container
.next

# Git directory - not needed in container
.git
.gitignore

# Local environment files - use docker-compose environment instead
.env
.env.local
.env.development
.env.production

# SQLite database files (if any from local development)
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal

# IDE and editor files
.vscode
.idea
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Test files - not needed for production build
**/__tests__
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
coverage

# Documentation
*.md
!README.md

# Docker files (no need to copy these into the image)
Dockerfile
docker-compose.yml
docker-compose*.yml

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript build info
*.tsbuildinfo
