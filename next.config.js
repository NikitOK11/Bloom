/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable experimental features if needed in the future
  // For MVP, we keep it simple with default settings
};

module.exports = nextConfig;
